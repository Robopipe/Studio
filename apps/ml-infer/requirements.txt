# onnxruntime is intentionally NOT pinned here — the Dockerfile installs it
# via the ORT_PACKAGE build arg so the CPU image (predict service) gets
# `onnxruntime` and the GPU image (confidence-report job) gets
# `onnxruntime-gpu`, both pinned to the same 1.23.0 version. See Dockerfile.
#
# Pins below match apps/ml-yolo so wheels resolve identically and we don't
# drag numpy 2.x in via a transitive bump.
opencv-python==4.11.0.86
numpy==1.26.4

# CA bundle for HTTPS downloads — Python.org macOS builds don't ship one,
# so urllib SSL handshakes fail without this.
certifi==2024.8.30

# Cloud Run server — only needed when running app.server, not the CLI.
# uvicorn[standard] adds uvloop + httptools, which knocks ~30-40% off
# request overhead vs the default asyncio event loop. Negligible install
# cost (a few MB) and worth it for warm-call latency.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.5

# Polygon IoU for the confidence-report batch job.
shapely==2.0.6
